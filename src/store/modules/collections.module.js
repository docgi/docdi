const state = {
  collections: []
};

const getters = {
  getCollections(state) {
    return state.collections;
  }
};

const actions = {
};

const mutations = {
};

export default {
  state,
  mutations,
  actions,
  getters
};
